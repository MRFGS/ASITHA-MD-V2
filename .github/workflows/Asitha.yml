name: SxL-ASITHS

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours

concurrency:
  group: nodejs-app  # This ensures that jobs are grouped
  cancel-in-progress: true  # This will cancel any in-progress job

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    env:
      SESSION_ID: "𝙰𝚂𝙸𝚃𝙷𝙰-𝙼𝙳=frQzHSyB#XgiOwyhQyD9i_IGx8tysS4Wbf4KEOvgu-7l8H6qhOSk"
      GITHUB_AUTH_TOKEN: "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"
       GITHUB_USERNAME: "MRFGS"

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm install --legacy-peer-deps

    - name: Start application
      run: npm start
